@page
@model SportEU.Pages.Groups.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GroupData[0].GroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupData[0].AthleteId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupData[0].Coach)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GroupData[0].RowVersion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.GroupData) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AthleteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Coach.FirstMidName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RowVersion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
